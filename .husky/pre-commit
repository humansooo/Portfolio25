. "$(dirname "$0")/common.sh"

husky_banner "Pre-commit"

if ! bun run format:check; then
  husky_step "Prettier (write)" bun run format
fi
husky_step "TypeScript" bun run typecheck

husky_footer_ok "Pre-commit"
